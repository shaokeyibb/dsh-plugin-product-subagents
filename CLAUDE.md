# CLAUDE.md

The agent guidance for this repository lives in [AGENTS.md](AGENTS.md) — one
canonical file for every agent (Claude Code, Codex, DeepSeek Harness agents, …).

@AGENTS.md

> This file used to be a symlink to `AGENTS.md`. It is a regular file because
> the DSH STORE fixed-Commit contract requires the pinned source tree to contain
> only ordinary files — a symlink makes the install source non-reproducible and
> defers automatic updates. Keep the guidance itself in `AGENTS.md`.
